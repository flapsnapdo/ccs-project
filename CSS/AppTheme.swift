import SwiftUI

struct AppTheme {
    static let background = Color.black
    static let secondaryBackground = Color(red: 0.10, green: 0.10, blue: 0.10)
    static let cardBackground = Color(red: 0.14, green: 0.14, blue: 0.14)

    static let accent = Color(red: 0.96, green: 0.78, blue: 0.22)
    static let accentSoft = Color(red: 0.96, green: 0.78, blue: 0.22).opacity(0.15)

    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.7)

    static let lightCard = Color.white
    static let darkText = Color.black
}
